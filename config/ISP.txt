ena
conf t

# Đặt tên cho Router
host ISP

# Kích hoạt định tuyến IPv6
ipv6 unicast-routing

# Cấu hình Interface Serial 0/3/0 (Kết nối đến mạng khác hoặc mạng ngoài)
int se0/3/0
  ip address 203.0.114.2 255.255.255.252
  ipv6 address 2001:db8:acad:100::2/64
  ipv6 address fe80:5::1 link-local
  no shutdown

# Cấu hình Route Mặc Định cho IPv4 (đưa tất cả lưu lượng không xác định tới gateway)
ip route 0.0.0.0 0.0.0.0 se0/3/0

# Lưu cấu hình
end
wr
