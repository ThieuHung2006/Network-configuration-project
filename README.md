# Network-configuration-project
A network simulation project for a 3-layer enterprise network model (Core, Distribution, and Access). It includes the configuration of routing protocols (OSPF), VLANs, inter-VLAN routing, and DHCP services for end devices. The project demonstrates how to build and manage a secure and segmented network in an enterprise environment.
